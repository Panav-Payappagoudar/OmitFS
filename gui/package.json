{
  "name": "omitfs-gui",
  "version": "0.3.0",
  "private": true,
  "description": "OmitFS Desktop — intent-driven local semantic search",
  "scripts": {
    "dev":   "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0",
    "@tauri-apps/plugin-global-shortcut": "^2.0.0"
  }
}
